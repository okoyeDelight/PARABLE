# GitHub sync status

## Day 1

Pushed to GitHub:
- product architecture
- PostgreSQL Day 1 schema
- Kysely database helper/types
- project GET/POST endpoint source
- repository README and product principles

Still hosted only in the live Floot project at this checkpoint:
- the current premium dashboard page source/CSS
- Floot's seeded UI-kit files

Reason: the Floot workspace hit its daily build/read action limit before the final source mirror could be extracted. The live project itself is safe and checkpointed as **Day 1 — Studio foundation**. The next sync should copy the remaining custom UI source after the Floot action limit resets.
